ktor {
    deployment {
        port = 8080
    }
    application {
        modules = [ dev.skorobogatov.ApplicationKt.module ]
    }
}

claude {
    apiKey = "test-api-key"
    apiUrl = "https://api.anthropic.com/v1/messages"
    model = "claude-sonnet-4-20250514"
    maxTokens = 1024
}
